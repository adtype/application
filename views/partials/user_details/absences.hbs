
<div class="col-md-9">
  <form method="POST" action="/users/edit/{{employee.id}}/">

  <input type="hidden" name="back_to_absences" value="1">
  <input type="hidden" name="name" value="{{employee.name}}">
  <input type="hidden" name="lastname" value="{{employee.lastname}}">
  <input type="hidden" name="email_address" value="{{employee.email}}">
  <input type="hidden" name="department" value="{{employee.DepartmentId}}">
  <input type="hidden" name="admin" value="{{#if employee.admin}}1{{else}}0{{/if}}">
  <input type="hidden" name="start_date" value="{{as_date employee.start_date}}">
  <input type="hidden" name="end_date" value="{{as_date employee.end_date}}">


  <div class="form-group">
    <label for="days_remaining_inp" class="control-label">Días restantes</label>
    <div class="date">
      <input class="form-control" id="days_remaining_inp" type="text" readonly value="{{#with employee}}{{ this.calculate_number_of_days_available_in_allowance}} de un total de {{this.calculate_total_number_of_days_n_allowance }}{{/with}}">
    </div>
  </div>

  <div class="form-group">
    <label for="adjustment_inp" class="control-label">Ajustes de días</label>
    <div class="input-group col-md-4">
      <input class="form-control" id="adjustment_inp" type="number" step="0.5" name="adjustment" value="{{#if employee.adjustment }}{{employee.adjustment}}{{else}}0{{/if}}" aria-describedby="adjustment_help">
      <span class="input-group-addon">días de trabajo</span>
    </div>
    <div id="adjustment_help" class="help-block">
      <div>Ajusta los días restantes para este usuario</div>
      <div>También puede ser negativo.</div>
    </div>
  </div>

  <div class="form-group">
    <div class="col-md-12">
      <button id="save_changes_btn" type="submit" class="btn btn-success pull-right">Guardar cambios</button>
    </div>
  </div>
  </form>

  <div class="main-row_header">&nbsp;</div>

  {{> user_requests leaves=leaves no_header=1}}
</div>
